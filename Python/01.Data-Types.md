# Data Types and basic methods
### 1) String
#### String: plain texts, use "" to represent
```Shell
character_name = "Mike"
print("he liked the name " + character_name + ".")
```
he linked the name Mike.

#### Basic methods
```Shell
# case 1 and 2
print("Giraffe\nAcademy")
print("Giraffe\"Academy")

phrase = "Giraffe Academy"
print(phrase + " is cool")

# case 3
phrase = "Giraffe Academy"
print(phrase.lower())

phrase = "Giraffe Academy"
print(phrase.islower())

phrase = "Giraffe Academy"
print(phrase.upper().isupper())

# case 4: figure out length of strings
phrase = "Xuzhi Wei"
print(len(phrase))

# case 5:
phrase = "Giraffe Academy"
print(phrase[4])

# case 6: index funciton
phrase = "Giraffe Academy"
print(phrase.index("G"))

# case 7: replace
phrase = "Giraffe Academy"
print(phrase.replace("Giraffe", "Elephant"))

# Conclusion:
# 1) \n: words behind start appears in a new line
# 2) \": print out a ".
# 3) .lower() / .upper(): make strings shows in all lower/upper cases
#  a) .islower(): check if the text is lower
# 4) len(): to calculate a length of a string
# 5) [0]: to only show the first letter of a text
#  a) string gets index start with 0
# 6) .index(""): tells what position a number or phrase is located
# 7) .replace("a", "b"): replace a with b
```

### 2) Number
```Shell
# basic operator

# 1) modulo operator: It returns the remainder of dividing the left hand operand by right hand operand.
# 2) str(): convert number into string
# 3) pow(a, b): calculate a square b
print(pow(5, 2))

# 4) max(): show which number is higher
print(max(5, 2))

# 5) abs(): absolute value
my_num = -5
print(abs(my_num))
```
25
<br>5
<br>5

```Shell
# case 1
print(3 * 4 + 5)
print(3 * (4+5)) # order of calculation

# case 2: modulo operator %
print(10 % 4)

# case 3: str()
my_num = 5
print(str(my_num) + " my favorite number")

# case 4: abs (absolute value)
my_num = -5
print(abs(my_num))

# case 5: pow()
print(pow(5, 2))

# case 6: max() / min()
print(max(5, 2))

# case 7: round()
print(round(5.2))

# case 8: import code; floor(), ceil(), sqrt()
from math import *
print(floor(3.7))
print(ceil(3.2))
print(sqrt(3.8))


# conclusion
# 2) modulo operator: It returns the remainder of dividing the left hand operand by right hand operand.
# 3) str(): convert number into string
# 5) pow(a, b): calculate a square b
# 6) max(): show which number is higher
# 8): from math import *: give access of more math functions
#  a) floor(a.b): chop off decimal point b
#  b) ceil(): round number up no matter what
```

### 3) Boolean value
#### Boolean value: True or False
